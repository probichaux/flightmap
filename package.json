{
  "name": "flightmap",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy .",
    "dev": "wrangler pages dev . --port 8000"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
